# ------------------------------------------------------------------
# Core Python / Poetry
# ------------------------------------------------------------------
*.py[cod]
__pycache__/
.venv/
.env/

# Poetry virtual-envs created outside the project (safety)
# (uncomment if you keep them in-project)
# .venv/

# ------------------------------------------------------------------
# macOS & Editor junk
# ------------------------------------------------------------------
.DS_Store
.Spotlight-V100
.Trashes
.idea/                 # JetBrains / PyCharm
.vscode/               # VS Code settings
*.swp                  # Vim swap files
*.swo
*.sublime-*

# ------------------------------------------------------------------
# Build / runtime artefacts
# ------------------------------------------------------------------
*.log
*.concordia.csv        # result files regenerated each run
example_data/test_results/    # benchmark test results directory

# Evaluation outputs
eval/results/*                 # Ignore all content within the main results directory
!eval/example_outputs/         # DO track the specific example outputs directory
# eval/benchmark_results/      # Obsolete: directory removed
# eval/scripts/eval/results/   # Obsolete: directory removed

# Hugging Face cache (weights are huge; re-downloaded on demand)
.cache/
Library/Caches/huggingface/

# ------------------------------------------------------------------
# KEEP these (do NOT ignore)
# ------------------------------------------------------------------
# example_data/            ← we DO commit sample CSVs
# poetry.lock              ← keep for deterministic installs
